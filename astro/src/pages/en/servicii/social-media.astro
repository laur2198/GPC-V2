---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../../components/Breadcrumbs.astro';
import ServiceDetail from '../../../components/ServiceDetail.astro';
import { type Locale, t, localizedPath } from '../../../utils/i18n';

const locale: Locale = 'en';
const meta = t<{ title: string; description: string }>(locale, 'services.socialMedia.meta');

const breadcrumbs = [
  { name: 'Services', href: localizedPath(locale, '/servicii') },
  { name: "Social Media", href: localizedPath(locale, '/servicii/social-media') },
];

const serviceSchema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  '@id': 'https://greenpheonixconcept.com/en/servicii/social-media#service',
  name: "Social Media — Strategy, Content and Community",
  provider: { '@id': 'https://greenpheonixconcept.com/#organization' },
  serviceType: "Social Media Marketing",
  category: "Content Marketing",
  description: meta.description,
  areaServed: [
    { '@type': 'Country', name: 'Romania' },
    { '@type': 'Country', name: 'Italy' },
  ],
  audience: {
    '@type': 'BusinessAudience',
    audienceType: 'Small and medium businesses',
  },
  offers: {
    '@type': 'Offer',
    availability: 'https://schema.org/InStock',
    priceCurrency: 'EUR',
    priceSpecification: { '@type': 'PriceSpecification', priceCurrency: 'EUR' },
  },
  hasOfferCatalog: {
    '@type': 'OfferCatalog',
    name: "Social Media Services",
    itemListElement: ["Content strategy și pillars","Content calendar 30 zile","Community management","Reels și carousels production"].map((n) => ({
      '@type': 'Offer',
      itemOffered: { '@type': 'Service', name: n },
    })),
  },
};
---

<BaseLayout title={meta.title} description={meta.description} locale={locale} breadcrumbs={breadcrumbs} jsonLd={[serviceSchema]}>
  <Breadcrumbs locale={locale} items={breadcrumbs} />
  <ServiceDetail locale={locale} service="socialMedia" />
</BaseLayout>
